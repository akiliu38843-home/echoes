# 环境变量样例（复制为 .env.local 本地用；部署时填到 Render 控制台）。
# 真实密钥【绝不】提交到 git。

# LLM（DeepSeek，OpenAI 兼容）
OPENAI_API_KEY=sk-your-deepseek-key
OPENAI_BASE_URL=https://api.deepseek.com/v1
HINDCAST_MODEL=deepseek-chat

# Manifesto Project（政治派语料，可选）
MANIFESTO_API_KEY=

# CORS：前端单独部署（Vercel）时，收紧为你的前端域名，逗号分隔；
# 例：ALLOWED_ORIGINS=https://echoes.vercel.app
ALLOWED_ORIGINS=*
